# Github-PR-Analyzer
GitHub App that scores every PR's risk (0–100) using deterministic signals + LLM-found issues — the score is always computed, never assigned by the model. Posts as a Check Run, sticky comment, and inline review comments.
